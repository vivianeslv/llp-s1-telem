#include <stdio.h>

int main() {
    // Usando um loop for para imprimir números pares entre 1 e 50
    printf("Números pares entre 1 e 50:\n");
    for (int i = 2; i <= 50; i += 2) {
        printf("%d\n", i);
    }
    return 0;
}
